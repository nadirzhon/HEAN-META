---
name: rebalance-portfolio
description: Запускает принудительную пере-балансировку капитала между стратегиями.
disable-model-invocation: true
user-invocable: true
allowed-tools: Bash
---
## Objective
Вручную запустить процесс пере-распределения капитала в `StrategyCapitalAllocator`.

## Command
```bash
# Эта команда должна быть реализована в API
# curl -X POST http://localhost:8000/api/v1/portfolio/rebalance
echo "Функционал ручной пере-балансировки через API еще не реализован. Это - плейсхолдер."
```
