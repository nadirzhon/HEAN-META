# Диагностический отчет: Почему нет сделок сегодня

**Дата:** 03.01.2026  
**Время:** 11:13 UTC

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА: Нет данных о ценах

### Основная проблема

Система **не получает корректные цены** от Bybit WebSocket, что блокирует генерацию сигналов и сделок.

**Симптомы:**
- Множественные предупреждения: `Invalid lastPrice for BTCUSDT: None, skipping tick`
- Множественные предупреждения: `Invalid lastPrice for ETHUSDT: None, skipping tick`
- WebSocket постоянно переподключается
- **Сделок сегодня: 0**
- **Прибыль сегодня: $0.00**

### Текущая конфигурация

```
TRADING_MODE: live
IS_LIVE: True
DRY_RUN: False
SYMBOLS: ['BTCUSDT', 'ETHUSDT']
FUNDING_ENABLED: True
BASIS_ENABLED: True
IMPULSE_ENABLED: True
```

### Статус компонентов

✅ **Работает:**
- Система запущена и работает
- WebSocket подключен к Bybit mainnet
- Подписка на ticker для BTCUSDT и ETHUSDT активна
- Стратегии включены (Funding, Basis, Impulse)
- Paper broker активен (исполнение ордеров)

❌ **Не работает:**
- Получение цен от Bybit WebSocket (lastPrice = None)
- Генерация сигналов (нет цен → нет сигналов)
- Размещение ордеров (нет сигналов → нет ордеров)

## 🔍 Детальный анализ

### 1. Проблема с WebSocket

**Логи показывают:**
```
WARNING | hean.exchange.bybit.ws_public | Invalid lastPrice for BTCUSDT: None, skipping tick
WARNING | hean.exchange.bybit.ws_public | Invalid lastPrice for ETHUSDT: None, skipping tick
```

**Причина:**
- Bybit WebSocket отправляет сообщения, но поле `lastPrice` в данных равно `None`
- Это может быть из-за:
  1. Изменения формата API Bybit
  2. Неправильной подписки на канал
  3. Проблем с самим WebSocket соединением

### 2. Цепочка блокировок

```
Нет цен → Нет тиков → Нет сигналов → Нет ордеров → Нет сделок → Нет прибыли
```

### 3. WebSocket переподключения

Логи показывают частые переподключения:
```
WARNING | WebSocket connection closed, reconnecting (attempt 1/10)...
INFO    | Bybit public WebSocket connected to mainnet
```

Это указывает на нестабильное соединение.

## 💡 Решения

### Решение 1: Проверить формат сообщений от Bybit

Bybit мог изменить формат API. Нужно проверить:
1. Какие данные реально приходят в WebSocket сообщениях
2. Правильно ли парсится поле `lastPrice`
3. Возможно, нужно использовать другое поле (например, `markPrice` или `indexPrice`)

**Действие:**
```python
# Добавить логирование в ws_public.py для отладки
logger.debug(f"Received ticker data: {ticker_data}")
```

### Решение 2: Использовать альтернативный источник цен

Если WebSocket не работает, можно:
1. Использовать HTTP API для получения цен (polling)
2. Переключиться на синтетический price feed для тестирования
3. Использовать другой канал WebSocket (например, trade вместо ticker)

**Действие:**
```bash
# Временно переключиться на paper режим с синтетическим feed
export TRADING_MODE=paper
docker-compose restart hean
```

### Решение 3: Проверить подписку на канал

Возможно, подписка настроена неправильно. Проверить:
1. Правильный ли формат подписки для Bybit v5 API
2. Правильный ли topic используется
3. Нужна ли авторизация для public WebSocket

### Решение 4: Использовать HTTP API как fallback

Добавить HTTP polling для получения цен, если WebSocket не работает:
```python
# В price_feed.py добавить fallback на HTTP API
if websocket_failed:
    use_http_polling()
```

## 📊 Текущая статистика

- **Дневной PnL:** $0.00
- **Текущий капитал:** $400.00
- **Начальный капитал:** $30.53
- **Общий PnL:** +$369.47 (+1210.19%)
- **Сделок сегодня:** 0
- **Открытых позиций:** 0

## 🎯 Рекомендации

### Немедленные действия:

1. **Проверить логи WebSocket детально:**
   ```bash
   docker-compose logs hean | grep -A 5 "ticker\|lastPrice" | tail -50
   ```

2. **Проверить формат сообщений:**
   - Добавить debug логирование в `ws_public.py`
   - Посмотреть, что реально приходит от Bybit

3. **Временно переключиться на paper режим:**
   ```bash
   # В .env или через переменные окружения
   export TRADING_MODE=paper
   docker-compose restart hean
   ```

4. **Проверить документацию Bybit API:**
   - Убедиться, что формат подписки правильный
   - Проверить, не изменился ли API

### Долгосрочные улучшения:

1. Добавить fallback на HTTP API для получения цен
2. Добавить более детальное логирование WebSocket сообщений
3. Добавить health check для price feed
4. Добавить метрики для отслеживания проблем с ценами

## 🔧 Команды для диагностики

```bash
# Проверить статус системы
python3 get_today_profit.py

# Проверить логи WebSocket
docker-compose logs hean | grep -i "websocket\|ticker\|price" | tail -50

# Проверить конфигурацию
python3 -c "from hean.config import settings; print(f'Mode: {settings.trading_mode}, Live: {settings.is_live}')"

# Перезапустить систему
docker-compose restart hean

# Переключиться на paper режим
export TRADING_MODE=paper
docker-compose restart hean
```

## 📝 Вывод

**Основная причина отсутствия сделок:** Система не получает корректные цены от Bybit WebSocket, что блокирует всю цепочку: цены → сигналы → ордера → сделки.

**Приоритет:** Высокий - без цен система не может работать.

**Следующий шаг:** Проверить формат сообщений от Bybit и исправить парсинг цен, либо переключиться на альтернативный источник цен.

