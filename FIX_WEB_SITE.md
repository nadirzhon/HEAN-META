# 🔧 Исправление: отображается старый фронтенд

## Что происходит
Вместо нового дизайна (Vite/React из `apps/ui`) открывается старый сайт/кэш.

## Почему
Образ или контейнер `hean-ui` собран со старым кодом или браузер использует кэш.

## Быстрое решение

```bash
cd /Users/macbookpro/Desktop/HEAN
./rebuild_web.sh   # остановит ui, пересоберёт без кэша и запустит заново
```

## Ручная пересборка

```bash
# Остановить и удалить контейнер
docker-compose stop ui
docker-compose rm -f ui

# Удалить старый образ
docker rmi hean-ui

# Пересобрать без кэша
docker-compose build --no-cache ui

# Запустить
docker-compose up -d ui

# Проверить логи
docker logs hean-ui --tail 30
```

## Проверка

```bash
docker ps | grep hean-ui
docker logs hean-ui
curl http://localhost:3000/
```

Откройте http://localhost:3000 — должен загрузиться новый интерфейс с секциями Overview / Decisions / Strategies.

## Если проблема остаётся
1. Очистите кэш браузера (Cmd/Ctrl + Shift + R).
2. Убедитесь, что порт 3000 свободен: `lsof -i :3000`.
3. Соберите образ напрямую из `apps/ui`:  
   `docker build -t hean-ui apps/ui`.

## Что изменилось
- Старый фронтенд: legacy control-center / статические HTML.
- Новый фронтенд: Vite/React (`apps/ui`) с дизайном Real-time Trading Interface.
