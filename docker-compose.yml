services:
  # Backend API
  api:
    image: hean-api:latest
    build:
      context: .
      dockerfile: api/Dockerfile
      args:
        - BUILDKIT_INLINE_CACHE=1
    container_name: hean-api
    env_file:
      - backend.env
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - PYTHONPATH=/app/src
      - FUNDING_HARVESTER_ENABLED=true
      - BASIS_ARBITRAGE_ENABLED=true
      - IMPULSE_ENGINE_ENABLED=true
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./README.md:/app/README.md:ro
    command: uvicorn hean.api.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    ports:
      - "8000:8000"  # API endpoint
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - hean-network
    depends_on:
      redis:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

  # Backend API (development with hot-reload)
  api-dev:
    image: hean-api-dev:latest
    profiles: ["dev"]
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    container_name: hean-api-dev
    env_file:
      - backend.env
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - PYTHONPATH=/app/src
      - FUNDING_HARVESTER_ENABLED=true
      - BASIS_ARBITRAGE_ENABLED=true
      - IMPULSE_ENGINE_ENABLED=true
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./README.md:/app/README.md:ro
    command: uvicorn hean.api.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/src
    restart: unless-stopped
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - hean-network
    depends_on:
      redis:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # React/Vite UI (production, served via nginx)
  ui:
    image: hean-ui:latest
    build:
      context: ./apps/ui
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${VITE_API_BASE:-/api}
        VITE_WS_URL: ${VITE_WS_URL:-/ws}
    container_name: hean-ui
    env_file:
      - ui.env
    ports:
      - "3000:80"
    networks:
      - hean-network
    depends_on:
      - api
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # React/Vite UI (hot-reload dev profile)
  ui-dev:
    image: hean-ui-dev:latest
    profiles: ["dev"]
    build:
      context: ./apps/ui
      dockerfile: Dockerfile.dev
    container_name: hean-ui-dev
    environment:
      - VITE_API_BASE=http://localhost:8000
      - VITE_WS_URL=ws://localhost:8000/ws
    ports:
      - "5173:5173"
    networks:
      - hean-network
    depends_on:
      - api
    restart: unless-stopped
    volumes:
      - ./apps/ui/src:/app/src
      - ./apps/ui/index.html:/app/index.html
      - ./apps/ui/vite.config.ts:/app/vite.config.ts
      - ./apps/ui/postcss.config.mjs:/app/postcss.config.mjs

  # Redis for atomic state sharing between C++ and Python
  redis:
    image: redis:7-alpine
    container_name: hean-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - hean-network
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

networks:
  hean-network:
    driver: bridge

volumes:
  redis-data:
