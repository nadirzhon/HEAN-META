═══════════════════════════════════════════════════════════════════
🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С DOCKER
═══════════════════════════════════════════════════════════════════

Контейнер hean-symbiont-testnet не появился в Docker Desktop.
Давайте найдем и исправим проблему!

═══════════════════════════════════════════════════════════════════
🔍 ШАГ 1: ДИАГНОСТИКА
═══════════════════════════════════════════════════════════════════

Выполните эти команды в терминале (в директории проекта):

1️⃣  Проверка статуса всех контейнеров:
    ────────────────────────────────────────────────────────────
    docker compose ps -a
    ────────────────────────────────────────────────────────────

2️⃣  Проверка логов (если контейнер был запущен):
    ────────────────────────────────────────────────────────────
    docker compose logs symbiont-testnet
    ────────────────────────────────────────────────────────────

3️⃣  Проверка сборки образа:
    ────────────────────────────────────────────────────────────
    docker images | grep symbiont
    ────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════
🛠️  ШАГ 2: ПОЛНАЯ ПЕРЕСБОРКА
═══════════════════════════════════════════════════════════════════

Если образ не собрался или есть ошибки, выполните:

    # Удалить старый образ (если есть)
    docker compose down symbiont-testnet
    docker rmi hean-symbiont:latest 2>/dev/null

    # Пересобрать с нуля БЕЗ кэша
    docker compose build --no-cache symbiont-testnet

    # Запустить
    docker compose up symbiont-testnet


═══════════════════════════════════════════════════════════════════
⚡ ШАГ 3: БЫСТРЫЙ ЗАПУСК (если образ собран)
═══════════════════════════════════════════════════════════════════

Если образ уже собран (видите его в `docker images`):

    docker compose up symbiont-testnet


═══════════════════════════════════════════════════════════════════
🐛 ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ
═══════════════════════════════════════════════════════════════════

ПРОБЛЕМА 1: "Error: no such file or directory: requirements.txt"
────────────────────────────────────────────────────────────────────
РЕШЕНИЕ: Убедитесь что вы в директории HEAN:
    cd /path/to/HEAN
    ls requirements.txt  # Должен показать файл


ПРОБЛЕМА 2: "Cannot install pybit" или другие зависимости
────────────────────────────────────────────────────────────────────
РЕШЕНИЕ: Исправим requirements.txt, удалив проблемную строку:
    # Откройте requirements.txt и закомментируйте asyncio:
    # asyncio>=3.4.3  # Встроен в Python, не нужен


ПРОБЛЕМА 3: Контейнер запустился и сразу упал
────────────────────────────────────────────────────────────────────
РЕШЕНИЕ: Смотрите логи:
    docker compose logs --tail=100 symbiont-testnet

Частые причины:
  • Ошибка импорта Python модулей
  • Ошибка подключения к Bybit (403 Forbidden)
  • Отсутствует .env.symbiont


ПРОБЛЕМА 4: "Connection refused" к Bybit
────────────────────────────────────────────────────────────────────
ПРИЧИНА: Контейнер не может подключиться к интернету
РЕШЕНИЕ:
    # Проверьте подключение из контейнера:
    docker compose run --rm symbiont-testnet ping -c 3 api-testnet.bybit.com

    # Если не работает, проверьте настройки сети Docker Desktop


ПРОБЛЕМА 5: Контейнер работает, но ничего не выводит
────────────────────────────────────────────────────────────────────
РЕШЕНИЕ: Запустите в интерактивном режиме:
    docker compose up symbiont-testnet
    (без -d флага)


═══════════════════════════════════════════════════════════════════
🔄 АЛЬТЕРНАТИВА: ЗАПУСК БЕЗ DOCKER
═══════════════════════════════════════════════════════════════════

Если Docker не работает, можете запустить напрямую:

    # 1. Установите зависимости
    pip install pybit websockets aiohttp python-dotenv

    # 2. Запустите скрипт
    python3 live_testnet_real.py

Это должно работать на вашей машине!


═══════════════════════════════════════════════════════════════════
📝 ШАГ 4: ОТПРАВЬТЕ МНЕ ВЫВОД
═══════════════════════════════════════════════════════════════════

Скопируйте и отправьте мне вывод этих команд:

    echo "=== 1. Docker Images ==="
    docker images | grep symbiont

    echo "=== 2. Container Status ==="
    docker compose ps -a | grep symbiont

    echo "=== 3. Build Output (last 50 lines) ==="
    docker compose build symbiont-testnet 2>&1 | tail -50

    echo "=== 4. Container Logs ==="
    docker compose logs --tail=50 symbiont-testnet

Я увижу точную проблему и помогу исправить!


═══════════════════════════════════════════════════════════════════
✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (когда все работает)
═══════════════════════════════════════════════════════════════════

В Docker Desktop вы должны увидеть:
  • Контейнер: hean-symbiont-testnet
  • Статус: Running (зеленая точка)
  • Порты: (нет открытых портов)

В логах должно быть:
  ✅ REAL DATA RECEIVED:
     Symbol: BTCUSDT
     Price: $XX,XXX.XX

  ✅ RECEIVED 10 REAL KLINES:
     Candle  1: ...


═══════════════════════════════════════════════════════════════════
🎯 КРАТКАЯ ВЕРСИЯ (копируй-пасти)
═══════════════════════════════════════════════════════════════════

docker compose down
docker rmi hean-symbiont:latest 2>/dev/null
docker compose build --no-cache symbiont-testnet
docker compose up symbiont-testnet


═══════════════════════════════════════════════════════════════════
