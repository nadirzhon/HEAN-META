# 📊 Бэктест на 30 дней - Статус

**Дата запуска**: 2025-12-31  
**Статус**: ⏳ Выполняется

## 🔄 Текущий статус

Бэктест на 30 дней запущен и выполняется в фоновом режиме.

### Что происходит:
- Система симулирует торговлю за последние 30 дней
- Генерируются сигналы от всех стратегий
- Обрабатываются события и выполняются сделки
- Собираются метрики производительности

## 📋 Как проверить результаты

### Вариант 1: Использовать скрипт проверки
```bash
./check_backtest_results.sh
```

### Вариант 2: Проверить вручную
```bash
# Проверить, завершился ли процесс
ps aux | grep "hean.main backtest"

# Посмотреть последние строки лога
tail -50 backtest_30days_output.log

# Проверить файл результатов
cat backtest_results_30days.json
```

### Вариант 3: Запустить заново (если нужно)
```bash
python3 -m hean.main backtest --days 30 --out backtest_results_30days.json
```

## 📊 Что будет в результатах

После завершения бэктеста вы получите:

1. **Основные метрики:**
   - Начальный капитал
   - Финальный капитал
   - Общий PnL (прибыль/убыток)
   - Доходность в процентах

2. **Риск-метрики:**
   - Максимальный Drawdown
   - Sharpe Ratio
   - Profit Factor

3. **Торговая статистика:**
   - Всего сделок
   - Прибыльных сделок
   - Убыточных сделок
   - Win Rate (процент прибыльных сделок)

4. **Статистика по стратегиям:**
   - Funding Harvester
   - Basis Arbitrage
   - Impulse Engine

## ⏱️ Ожидаемое время выполнения

Бэктест на 30 дней может занять:
- **5-15 минут** в зависимости от количества событий
- Больше времени, если генерируется много сигналов

## 🔍 Мониторинг прогресса

Проверяйте прогресс командой:
```bash
tail -f backtest_30days_output.log
```

Ищите строки:
- `[run_backtest] Calculating metrics...` - бэктест завершается
- `Backtest completed` - бэктест завершен
- `FORCED_ASSERTIONS_PASSED` - проверки пройдены

## 📁 Файлы результатов

После завершения будут созданы:
- `backtest_results_30days.json` - JSON с полными результатами
- `backtest_30days_output.log` - полный лог выполнения

## ⚠️ Если бэктест завис

Если бэктест не завершается долгое время (>30 минут):

1. Остановите процесс:
```bash
pkill -f "hean.main backtest"
```

2. Проверьте логи на ошибки:
```bash
grep -i error backtest_30days_output.log
```

3. Попробуйте запустить на меньший период (7 дней):
```bash
python3 -m hean.main backtest --days 7
```

## ✅ После завершения

Когда бэктест завершится, используйте скрипт для просмотра результатов:
```bash
./check_backtest_results.sh
```

Или откройте JSON файл напрямую:
```bash
python3 -m json.tool backtest_results_30days.json | less
```

