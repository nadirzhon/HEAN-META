# Git Visual Assets

Place visual Git materials here:

- `git-workflow.svg` - static workflow image already tracked in repo
- `workflow-demo.gif` - short demo of branch -> commit -> push flow
- `before.png` - state before changes
- `after.png` - state after changes
- `make-demo-gif.sh` - helper script to record/render terminal GIF

These files are referenced from:

- `README.md`
- `docs/GIT_WORKFLOW.md`
