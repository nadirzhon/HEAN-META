# 🧬 HEAN SYMBIONT X - Docker Quick Start

## Быстрый запуск

### Метод 1: Использование скрипта (рекомендуется)

```bash
./run_symbiont_docker.sh
```

Скрипт предоставляет интерактивное меню:
1. **Full System Demo** - полная демонстрация без подключения к биржам
2. **Bybit Testnet** - подключение к реальному Testnet API
3. **Build** - пересборка Docker образов
4. **Logs** - просмотр логов
5. **Stop** - остановка контейнеров

---

### Метод 2: Прямые команды Docker Compose

#### 🎯 Полная демонстрация (offline)

Запуск симуляции всех 8 компонентов системы:

```bash
# Сборка образа
docker compose build symbiont-demo

# Запуск
docker compose up symbiont-demo
```

**Что вы увидите:**
- ✅ Создание популяции из 10 стратегий
- ✅ Симуляция рыночных данных (10 тиков BTC/USDT)
- ✅ Определение рыночного режима
- ✅ Эволюция стратегий с мутациями
- ✅ Распределение капитала $10,000
- ✅ Принятие торговых решений
- ✅ Проверка рисков
- ✅ Мониторинг KPI

---

#### 📡 Подключение к Bybit Testnet (live)

**Требования:** Валидные API ключи в `.env.symbiont`

```bash
# Сборка образа
docker compose build symbiont-testnet

# Запуск в фоновом режиме
docker compose up -d symbiont-testnet

# Просмотр логов
docker compose logs -f symbiont-testnet

# Остановка
docker compose stop symbiont-testnet
```

**Что система делает:**
- 🔄 Подключается к Bybit Testnet WebSocket
- 📊 Получает реальные данные BTC/USDT
- 🧠 Анализирует рыночный режим
- 🧬 Эволюционирует стратегии в реальном времени
- 💰 Управляет портфелем $10,000 (paper trading)
- 📝 Записывает все решения в immutable ledger

---

## 🔧 Конфигурация

### Файл `.env.symbiont`

```bash
# Bybit API (получить на testnet.bybit.com)
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here

# Режим работы
BYBIT_TESTNET=true
PAPER_TRADING=true

# Параметры торговли
INITIAL_CAPITAL=10000
MAX_POSITION_SIZE=1000
RISK_PER_TRADE=0.02
```

---

## 📊 Просмотр результатов

### Логи в реальном времени

```bash
# Все логи SYMBIONT X
docker compose logs -f symbiont-testnet

# Только последние 100 строк
docker compose logs --tail=100 symbiont-testnet

# С временными метками
docker compose logs -f -t symbiont-testnet
```

### Файлы данных

Все данные сохраняются на хосте:

```
./logs/           # Логи системы
./data/           # Исторические данные
  └── historical/ # OHLCV данные от Bybit
```

---

## 🛠️ Управление контейнерами

```bash
# Статус всех контейнеров
docker compose ps

# Остановить SYMBIONT X
docker compose stop symbiont-testnet symbiont-demo

# Запустить заново
docker compose start symbiont-testnet

# Полная перезагрузка
docker compose restart symbiont-testnet

# Удалить контейнеры (данные сохранятся)
docker compose down
```

---

## 🔍 Диагностика

### Проверка работы

```bash
# Запущен ли контейнер?
docker compose ps symbiont-testnet

# Потребление ресурсов
docker stats hean-symbiont-testnet

# Проверка сети
docker compose exec symbiont-testnet ping -c 3 api.testnet.bybit.com

# Зайти в контейнер
docker compose exec symbiont-testnet /bin/bash
```

### Частые проблемы

**1. "Cannot connect to Docker daemon"**
```bash
# Решение: запустите Docker Desktop
```

**2. "Invalid API credentials"**
```bash
# Решение: проверьте .env.symbiont
cat .env.symbiont | grep BYBIT_API
```

**3. "Container exits immediately"**
```bash
# Смотрите логи для деталей
docker compose logs symbiont-testnet
```

---

## 🚀 Архитектура Docker

### Сервисы

| Сервис | Описание | Порты | Режим |
|--------|----------|-------|-------|
| `symbiont-demo` | Offline демонстрация | - | one-shot |
| `symbiont-testnet` | Bybit Testnet trading | - | daemon |
| `api` | HEAN Backend API | 8000 | daemon |
| `ui` | HEAN Web Interface | 3000 | daemon |
| `redis` | State cache | 6379 | daemon |

### Ресурсы

**symbiont-testnet:**
- CPU: 0.5-2.0 cores
- Memory: 512MB-2GB
- Network: требуется доступ к api.bybit.com

**symbiont-demo:**
- CPU: 0.25-1.0 cores
- Memory: 256MB-1GB
- Network: не требуется

---

## 📈 Мониторинг производительности

### KPI системы

Система автоматически отслеживает:
- **Latency:** < 10ms (target)
- **Throughput:** 1000+ events/sec
- **CPU Usage:** < 50%
- **Memory:** < 1GB
- **Decision Rate:** 2-5 decisions/min

### Проверка здоровья

```bash
# Health check статус
docker inspect hean-symbiont-testnet | jq '.[0].State.Health'

# Время работы
docker compose ps --format "table {{.Name}}\t{{.Status}}\t{{.Ports}}"
```

---

## 🧪 Режимы работы

### 1. Demo Mode (symbiont-demo)
- ✅ Без API ключей
- ✅ Симуляция данных
- ✅ Быстрый запуск
- ❌ Нет реальных данных

### 2. Paper Trading (symbiont-testnet)
- ✅ Реальные данные Testnet
- ✅ Нулевой риск
- ✅ Полная функциональность
- ⚠️ Требуются API ключи

### 3. Production (будущее)
- ✅ Реальная торговля
- ⚠️ Реальные деньги
- ⚠️ Требует extensive тестирование

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Наблюдайте за логами** и убедитесь, что все работает
2. **Проверьте метрики** в `docker stats`
3. **Изучите данные** в `./data/historical/`
4. **Настройте параметры** в `.env.symbiont`
5. **Запустите бэктесты** для оптимизации

---

## 📚 Дополнительная информация

- **Полная документация:** `README.md`
- **Архитектура системы:** `ARCHITECTURE.md`
- **Roadmap:** `ROADMAP.md`
- **Результаты тестов:** `TEST_RUN_SUCCESS.md`

---

## 💬 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs -f symbiont-testnet`
2. Убедитесь, что Docker Desktop запущен
3. Проверьте API ключи в `.env.symbiont`
4. Убедитесь в доступе к интернету

---

**🧬 HEAN SYMBIONT X - Living, Breathing, Evolving Trading Organism**
