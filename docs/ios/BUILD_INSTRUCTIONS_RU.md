# 📦 ИНСТРУКЦИЯ ПО СБОРКЕ UI (После оптимизации)

## ✅ Статус оптимизации

Все исправления производительности **применены и сохранены** в исходниках:
- ✅ EventFeed - добавлен debounce и кэш
- ✅ AdvancedChart - добавлен useMemo и оптимизация
- ✅ OrdersTable - добавлен React.memo
- ✅ PositionsTable - добавлен React.memo
- ✅ StatusBar - оптимизирован

Полный отчёт: `PERFORMANCE_FIXES_REPORT.md`

---

## 🚀 СБОРКА НА ВАШЕМ КОМПЬЮТЕРЕ

### Вариант 1: Локальная сборка (Рекомендуется)

```bash
# Перейдите в папку UI
cd apps/ui

# Установите зависимости (на вашем компьютере должно работать)
npm install

# Соберите проект
npm run build

# Результат будет в папке dist/
```

### Вариант 2: Docker сборка

```bash
# Из корня проекта HEAN
cd apps/ui

# Создайте временный Dockerfile
cat > Dockerfile.build <<'EOF'
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EOF

# Соберите в Docker
docker build -f Dockerfile.build -t hean-ui-build .

# Скопируйте результат
docker create --name temp-build hean-ui-build
docker cp temp-build:/app/dist ./dist-new
docker rm temp-build

# Замените старый dist
mv dist dist-old
mv dist-new dist
```

### Вариант 3: Использование Vite напрямую

```bash
cd apps/ui

# Установите vite глобально
npm install -g vite

# Запустите dev-сервер с hot reload
npx vite

# Или соберите напрямую
npx vite build
```

---

## 🔍 ПРОВЕРКА ИЗМЕНЕНИЙ

После сборки проверьте, что все оптимизации работают:

### 1. Откройте браузер DevTools (F12)

### 2. Включите React DevTools профилировщик:
- Components → ⚙️ Settings → Profiler → "Record why each component rendered"

### 3. Проверьте EventFeed:
```
✅ Быстрый ввод в поиск - должен быть плавным
✅ Результаты появляются с задержкой ~300ms
✅ В консоли не должно быть JSON.stringify warnings
```

### 4. Проверьте график (AdvancedChart):
```
✅ При обновлении данных - нет мерцания
✅ В React Profiler - минимальное количество рендеров
```

### 5. Проверьте таблицы:
```
✅ При изменении других данных таблицы не мигают
✅ Обновляются только при изменении orders/positions
```

### 6. Общая производительность:
```
✅ FPS должен быть стабильным ~60fps
✅ Кнопки реагируют мгновенно
✅ Прокрутка плавная
```

---

## 📊 МЕТРИКИ ПРОИЗВОДИТЕЛЬНОСТИ

Используйте Lighthouse для измерения:

```bash
# Запустите собранную версию
cd apps/ui/dist
python3 -m http.server 8080

# В Chrome:
# DevTools → Lighthouse → Performance
```

**Ожидаемые результаты после оптимизации:**
- Performance Score: 85-95+
- First Contentful Paint: < 1.5s
- Time to Interactive: < 3s
- Total Blocking Time: < 200ms

---

## 🐛 ЕСЛИ СБОРКА НЕ РАБОТАЕТ

### Проблема: "vite: command not found"
```bash
# Убедитесь что node_modules установлены
ls node_modules | wc -l  # должно быть > 0

# Переустановите
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Cannot find module..."
```bash
# Очистите кэш
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "out of memory"
```bash
# Увеличьте память для Node.js
export NODE_OPTIONS="--max-old-space-size=4096"
npm run build
```

### Проблема: "403 Forbidden" (как сейчас)
```bash
# Попробуйте другой registry
npm config set registry https://registry.npmmirror.com
npm install

# Или используйте yarn
npm install -g yarn
yarn install
yarn build
```

---

## 📁 СТРУКТУРА ПОСЛЕ СБОРКИ

```
apps/ui/
├── dist/                    # Собранная версия (готова к deploy)
│   ├── index.html          # Главный HTML
│   └── assets/             # JS, CSS, images
│       ├── index-[hash].js
│       └── index-[hash].css
├── src/                     # Исходники (с нашими оптимизациями!)
│   └── app/
│       ├── components/
│       │   └── trading/
│       │       ├── EventFeed.tsx       ✅ ОПТИМИЗИРОВАН
│       │       ├── AdvancedChart.tsx   ✅ ОПТИМИЗИРОВАН
│       │       ├── OrdersTable.tsx     ✅ ОПТИМИЗИРОВАН
│       │       ├── PositionsTable.tsx  ✅ ОПТИМИЗИРОВАН
│       │       └── StatusBar.tsx       ✅ ОПТИМИЗИРОВАН
│       └── hooks/
│           └── useTradingData.ts       ℹ️ Можно улучшить батчинг
└── package.json
```

---

## 🚢 ДЕПЛОЙ

### Production сборка:
```bash
cd apps/ui
npm run build

# Результат в dist/ готов для:
# - Static hosting (Vercel, Netlify, GitHub Pages)
# - Docker image
# - Nginx/Apache
```

### Проверка размера бандла:
```bash
cd dist
du -sh .              # Общий размер
du -sh assets/*       # Размеры отдельных файлов
```

**Ожидаемые размеры после оптимизации:**
- JS: ~400-500 KB (gzipped ~120-150 KB)
- CSS: ~100-120 KB (gzipped ~20-30 KB)

---

## 💡 ДОПОЛНИТЕЛЬНЫЕ ОПТИМИЗАЦИИ

После успешной сборки можно добавить:

### 1. Code Splitting
```typescript
// В вашем роутере
const Dashboard = lazy(() => import('./Dashboard'));
```

### 2. Bundle Analyzer
```bash
npm install -D rollup-plugin-visualizer
# Добавьте в vite.config.ts
```

### 3. Compression
```bash
# В nginx.conf
gzip on;
gzip_types text/plain text/css application/json application/javascript;
```

---

## 📞 ПОДДЕРЖКА

Если возникнут проблемы:
1. Проверьте `PERFORMANCE_FIXES_REPORT.md` - там все детали
2. Убедитесь что все изменённые файлы на месте
3. Проверьте версию Node.js: `node -v` (должна быть >= 18)
4. Проверьте версию npm: `npm -v` (должна быть >= 9)

---

**Все оптимизации сохранены! Просто соберите проект локально.** 🚀
