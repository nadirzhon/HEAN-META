# HEAN — ДЕМОН ЛАПЛАСА ДЛЯ ФИНАНСОВОГО РЫНКА

## Единый Технический Документ v2.0

> *«Разумное существо, которое в определённый момент знало бы все силы, одушевляющие природу, и положение каждой её составной части — для него не было бы ничего неясного, и будущее, как и прошлое, было бы у него перед глазами»*
> — Пьер-Симон Лаплас, 1814

---

# ЧАСТЬ I: ФИЛОСОФИЯ И КОНЦЕПЦИЯ

## 1.1 Что такое HEAN

HEAN — это не бот. Не индикатор. Не сигнальный сервис.

HEAN — это **информационный термодинамический двигатель**, который видит рынок как физическую систему, раскладывает КАЖДОГО участника на типы, измеряет силы по законам термодинамики и извлекает максимально возможную прибыль по формуле:

> **ПРИБЫЛЬ ≤ ТЕМПЕРАТУРА РЫНКА × ИНФОРМАЦИЯ**

Это доказанный научный принцип (Szilard Engine, ICTP 2021, Econophysics 2023). HEAN — первый продукт, который применяет его на практике.

## 1.2 Пять Заповедей

1. **Не предсказывать цену — видеть силы.** Цена — следствие. Причина — действия участников и физика рынка.
2. **Торговать только неизбежность.** Не «вероятно пойдёт вверх», а «физика рынка ТРЕБУЕТ движения сюда».
3. **Быть над системой, а не внутри.** Мета-игра — видеть всех игроков, их намерения, и действовать зная всю партию.
4. **Максимум информации = максимум прибыли.** По формуле Сцилларда — чем больше информации, тем больше «работы» (денег) можно извлечь.
5. **AI не тильтует, не устаёт, не жадничает.** Это главное преимущество над человеком.

## 1.3 Научный Фундамент

### Szilard Engine и Финансовые Рынки

Физическая модель Сцилларда (информационный двигатель) напрямую применяется к финансовым рынкам:

- **Термодинамика:** нельзя извлечь свободную работу из физической системы без информации
- **Финансы:** нельзя извлечь прибыль из рынка без информационного преимущества
- **Модель Глостена-Милгрома:** описывает рынок, где информированные трейдеры взаимодействуют с маркетмейкером
- **Формула:** максимальная прибыль ≤ температура рынка × количество информации

Это доказано математически (Journal of Statistical Mechanics) и подтверждено эмпирически на данных Bitcoin LOB (Level 3 Order Book).

### Термодинамика Ордербука

Ордербук — это термодинамическая система:
- **Ордера = частицы** в физической системе
- **Кинетическая энергия** = агрессивность ордеров (скорость, частота)
- **Потенциальная энергия** = напряжение между bid и ask
- **Температура** = мера ликвидности и волатильности
- **Энтропия** = мера хаоса/порядка в рынке
- **Фазовые переходы** = крахи и взрывы

### Financial Second Law

> Любая торговая стратегия замкнутого цикла (купить → продать → вернуться к нулю) даёт неположительную ожидаемую прибыль — если нет информационного edge.

HEAN создаёт этот edge через многослойный анализ рынка.

## 1.4 Почему AI Побеждает Человека

| Человек-скальпер | HEAN AI |
|---|---|
| Устаёт после 4 часов | Работает 24/7 |
| Тильтует после 3 стопов | Нет эмоций |
| Видит 1-2 параметра | Видит 50+ параметров одновременно |
| Жадничает / боится | Чётко по правилам |
| Скорость: 300-500мс | Скорость: <5мс |
| Торгует 1-3 пары | Сканирует все пары |

---

# ЧАСТЬ II: 6 СЛОЁВ СИСТЕМЫ

## СЛОЙ 1: ГЛАЗА (Сбор Данных)

**Цель:** Собрать МАКСИМУМ информации о рынке в реальном времени.

### Источники данных

| Источник | Данные | API | Стоимость |
|----------|--------|-----|-----------|
| Bybit WebSocket | Цена, ордербук (50 уровней), сделки, ликвидации | `wss://stream.bybit.com` | Бесплатно |
| Bybit REST | Funding rate, Open Interest, Long/Short ratio | `api.bybit.com/v5` | Бесплатно |
| Binance WebSocket | Цена, ордербук, сделки (для корреляций) | `wss://stream.binance.com` | Бесплатно |
| Coinglass API | Карта ликвидаций, OI по биржам, funding по биржам | `open-api.coinglass.com` | $0-30/мес |
| Whale Alert / Arkham | Крупные переводы на/с бирж | API | Бесплатно |
| Fear & Greed Index | Сентимент рынка | `api.alternative.me` | Бесплатно |
| Twitter/X API | Сентимент, новости | API | Опционально |

### Структура данных

```
/data
├── orderbook/        # Снимки ордербука каждые 100мс
│   └── btcusdt/
│       └── 2025-02-07/
├── trades/           # Каждая сделка
├── liquidations/     # Каждая ликвидация
├── funding/          # Funding rate каждые 8 часов
├── oi/               # Open Interest каждые 5 минут
├── whale/            # Крупные переводы
└── sentiment/        # Fear & Greed, соцсети
```

**Результат Слоя 1:** HEAN видит ВСЁ что происходит на рынке в реальном времени.

---

## СЛОЙ 2: МОЗГ (AI Анализ — Двойной Мозг)

### Стратегический мозг: Claude API

Каждые 30 секунд (или при аномалии) система формирует «снимок рынка» и отправляет в Claude:

```
СНИМОК РЫНКА (пример):
- BTC: 94,247 | 24ч: -1.2% | 1ч: +0.3%
- Ордербук: bid/ask ratio 0.72 (продавцы доминируют)
- Дельта за 5 мин: -$2.4M (агрессивные продажи)
- Стена: 340 BTC на 95,200 (снимается по 20-30 BTC)
- Funding: +0.04% (лонги платят)
- OI: +2.3% за 6ч при боковике
- Ликвидации: $47M скоплены на 93,200
- Long/Short: 78/22
- Whale: 3,200 BTC переведены на Bybit за 2ч
- Fear & Greed: 71 (Жадность)
```

Claude возвращает:
```json
{
  "situation": "Ловушка для лонгистов. ММ снимает стену...",
  "forces": {
    "gravitational_pull": "DOWN → 93,200",
    "institutional_flow": "SHORT accumulation",
    "retail_sentiment": "Extreme greed = SELL signal"
  },
  "action": "WAIT for liquidation cascade trigger",
  "confidence": 0.82
}
```

### Тактический мозг: ONNX Runtime (v2)

Локальная нейросеть для мгновенных реакций (<5мс):

```python
import onnxruntime as ort
session = ort.InferenceSession("hean_classifier.onnx")
result = session.run(None, {
    "orderbook_imbalance": [...],
    "delta_flow": [...],
    "entropy": [...],
    "temperature": [...]
})
# → [0.87] = 87% вероятность каскада
```

| | Claude API | ONNX модель |
|---|---|---|
| Роль | Стратег | Рефлексы |
| Задержка | 1-3 сек | <5мс |
| Глубина | Полный анализ | Паттерн-матчинг |
| Стоимость | $5-20/мес | Бесплатно |
| Когда | Каждые 30 сек | Каждые 100мс |

**Стратегия:** Claude думает — ONNX реагирует. Как мозг и спинной мозг.

---

## СЛОЙ 3: ФИЗИКА (Термодинамика Рынка)

### Температура Рынка (Market Temperature)

```
T = KE / N

где:
KE = Σ (ΔP_i × V_i)²  — кинетическая энергия ордербука
N = количество активных уровней ордербука
```

**Интерпретация:**
- T высокая → рынок «горячий» → ликвидность есть, но volatile
- T низкая → рынок «холодный» → мало активности, спред широкий
- T падает резко → замораживание перед взрывом

### Энтропия Рынка (Market Entropy)

```
S = -Σ p_i × log(p_i)

где p_i = доля объёма на уровне i в ордербуке
```

**Интерпретация:**
- S высокая → ордера распределены равномерно → рынок в балансе
- S низкая → ордера сконцентрированы → кто-то готовит движение
- S резко падает → «пружина сжата» → взрыв неизбежен

### Формула Сцилларда

```
MAX PROFIT = T × I

где:
T = температура рынка (ликвидность/волатильность)
I = информация (в битах) = log₂(1/p) где p = вероятность исхода

Пример:
T = 0.05 (5% движение за цикл)
I = 3 бита (8 состояний, мы знаем точное)
MAX PROFIT = 0.05 × 3 = 15% от позиции
```

### Фазовые Переходы

Рынок проходит фазы как физическая система:

```
ЛЁД (боковик)
  │
  ├── T растёт, S падает
  │     → Сжатие (кто-то накапливает)
  │
  ▼
ВОДА (тренд)
  │
  ├── T высокая, S растёт
  │     → Нормальное движение
  │
  ▼
ПАР (каскад/крах)
  │
  ├── T взрывается, S максимум
  │     → Паника / эйфория
  │
  ▼
ЛЁД (новый боковик)
```

**HEAN ищет момент перехода ЛЁД → ВОДА** — максимальный edge при минимальном риске.

---

## СЛОЙ 4: РЕНТГЕН (Декомпозиция Участников)

AI кластеризует ВСЕ ордера в реальном времени на типы:

| Тип | Паттерн | Что означает |
|-----|---------|--------------|
| 🏦 **Маркетмейкер** | Симметричные лимитки, быстрые отмены, спуфинг | Контролирует спред, охотится на стопы |
| 🏛 **Институционал** | Крупные лимитки на одном уровне, iceberg orders | Набирает позицию, двигает рынок |
| 🤖 **Арбитражный бот** | Одновременные ордера на разных биржах, <10мс | Выравнивает цены между биржами |
| 📱 **Ритейл** | Маленькие маркет-ордера, кластеры на круглых числах | Пушечное мясо, торгует по индикаторам |
| 🐋 **Кит** | Разовый крупный ордер, часто маркет | Двигает рынок, часто инсайд |

**Мета-игра (Охотник на охотников):**
- ММ охотится на стопы ритейла → HEAN видит это и входит ВМЕСТЕ с ММ на развороте
- Институционал набирает позицию → HEAN видит iceberg и следует за ним
- Ритейл массово лонгует → HEAN готовит шорт

---

## СЛОЙ 5: ДЕМОН (Мета-Игра + Темпоральный Стек)

### Темпоральный Стек (5 уровней времени)

```
УРОВЕНЬ 5: МАКРО (дни-недели)
├── Тренд: Бычий/Медвежий/Боковик
├── Фаза цикла: Накопление/Распределение/Тренд
└── Доминирующая сила: Институционалы набирают

УРОВЕНЬ 4: СЕССИЯ (часы)
├── Текущая сессия: London Open
├── Режим: Breakout (после Азии)
└── Стратегия: Momentum

УРОВЕНЬ 3: ТАКТИКА (минуты)
├── Ситуация: ММ снимает стену на 95,200
├── Ордерфлоу: Агрессивные продажи ($2.4M delta)
└── Готовность: 4/5 сигналов сошлись

УРОВЕНЬ 2: ИСПОЛНЕНИЕ (секунды)
├── Точка входа: 93,250 (после пробоя ликвидаций)
├── Стоп: 93,800 (за предыдущий high)
└── R:R = 1:4.2

УРОВЕНЬ 1: МИКРО (миллисекунды)
├── Ордербук прямо сейчас
├── Тип ордера: Limit на 93,250
└── Проскальзывание: ожидаемое 0.02%
```

### Межрыночный Нервный Импульс

```
BTC двигается → через 200-2000мс → ETH двигается → через 500мс → альты

Эта задержка НЕ постоянная. Она зависит от:
- Режима рынка (паника / спокойствие)
- Времени суток (Азия / NY overlap)
- Текущей корреляции

AI учит задержки в реальном времени и торгует ОПЕРЕЖАЯ каскад.
```

### Темпоральный Арбитраж Эмоций

Новости двигают рынок волнами:
- 0-2 сек: алгоритмы (NLP-парсинг ключевых слов)
- 2-30 сек: первая волна трейдеров
- 30 сек - 5 мин: основная масса
- 5-30 мин: запоздавшие

Первая реакция почти всегда ПРЕУВЕЛИЧЕНА → fade the spike.

---

## СЛОЙ 6: IPHONE ПРИЛОЖЕНИЕ (HEAN UI)

### Экран 1: Dashboard (Главный)

```
┌─────────────────────────────────────┐
│  HEAN                    ●  Live    │
├─────────────────────────────────────┤
│                                     │
│    BTC/USDT    94,247.50            │
│    ████████░░  Уверенность: 82%     │
│                                     │
│  ┌─ ТЕМПЕРАТУРА ─┐ ┌─ ЭНТРОПИЯ ──┐ │
│  │   🌡 847       │ │   📊 0.34    │ │
│  │   ▲ +23%      │ │   ▼ -41%    │ │
│  │   ГОРЯЧИЙ     │ │   СЖАТИЕ    │ │
│  └───────────────┘ └─────────────┘ │
│                                     │
│  ⚡ СИГНАЛ: SHORT READY             │
│  Каскад ликвидаций → 93,200        │
│  R:R = 1:4.2 | Стоп: 0.6%          │
│                                     │
│  P&L Сегодня: +$347 (+4.2%)        │
│  Сделок: 3 (W:2 L:1)               │
│                                     │
│  [👁 МОЗГ] [🎯 АНОМАЛИИ] [⚡ ТОРГ.] │
└─────────────────────────────────────┘
```

### Экран 2: Мозг (Процесс Мышления)

Визуализация того, КАК AI думает в реальном времени:

```
┌──────────────────────────────────────┐
│  🧠 ПРОЦЕСС МЫШЛЕНИЯ          Live  │
├──────────────────────────────────────┤
│                                      │
│  14:32:07 — Обнаружена аномалия      │
│  "OI растёт при боковике. ММ         │
│   снимает стену на 95,200.           │
│   Институционал набирает шорт."      │
│                                      │
│  14:32:08 — Физика                   │
│  T: 847 → ГОРЯЧИЙ                    │
│  S: 0.34 → СЖАТИЕ (пружина)         │
│  Фаза: ЛЁД → ВОДА (переход)         │
│                                      │
│  14:32:09 — Рентген                  │
│  🏦 ММ: Снимает стену (ловушка)      │
│  🏛 Институционал: Шорт +$12M       │
│  📱 Ритейл: 78% лонг (жертвы)       │
│                                      │
│  14:32:10 — Решение                  │
│  ✅ 4/5 сигналов → SHORT             │
│  Вход: 94,200 | Стоп: 94,800        │
│  TP1: 93,600 | TP2: 93,200          │
│  Уверенность: 82% | R:R: 1:4.2      │
└──────────────────────────────────────┘
```

### Экран 3: Карта Гравитации

Визуализация притяжения цены к уровням ликвидаций:

```
┌──────────────────────────────────────┐
│  🌌 КАРТА ГРАВИТАЦИИ                 │
├──────────────────────────────────────┤
│                                      │
│  95,500  ░░░░░░░░ $12M ликвид.      │
│  95,200  ████████ СТЕНА (ловушка)    │
│  95,000  ░░░                         │
│  94,500  ░░                          │
│  94,247  ←── ЦЕНА СЕЙЧАС            │
│  94,000  ░░░                         │
│  93,500  ░░░░░░                      │
│  93,200  ████████████ $47M МАГНИТ    │
│  93,000  ░░░░░░░                     │
│  92,500  ░░░░ $8M ликвид.           │
│                                      │
│  → Притяжение ВНИЗ к 93,200         │
│    (максимальная гравитация)         │
└──────────────────────────────────────┘
```

### Экраны 4-8

- **Экран 4: Игроки (Рентген)** — реальное время кластеризации ордеров по типам участников
- **Экран 5: Темпоральный Стек** — 5 уровней времени, каждый обновляется
- **Экран 6: Агент (Сделка)** — детали текущей сделки, логика входа/выхода
- **Экран 7: Аномалии** — лента обнаруженных аномалий с severity
- **Экран 8: История** — все сделки, P&L кривая, статистика по стратегиям

---

# ЧАСТЬ III: УПРАВЛЕНИЕ РИСКАМИ

## Железные Правила (Не Нарушаются Никогда)

```
1. Максимальный убыток за день: -3% от депозита
   → Достигнут? СТОП на 24 часа. Без исключений.

2. Максимальный убыток за неделю: -7%
   → Достигнут? СТОП до понедельника.

3. Максимальный убыток на сделку: -1% от депозита
   → Размер позиции рассчитывается от стопа.

4. Максимум 3 сделки в убыток подряд
   → После 3-го стопа: пауза 2 часа, пересмотр стратегии.

5. Минимальный R:R = 1:2
   → Если R:R < 1:2 — НЕ входить, даже если сигнал 100%.
```

## Размер Позиции

```
Депозит: $300
Риск на сделку: 1% = $3

Пример с 20x leverage:
- Позиция: $6,000
- Стоп: 0.05% от цены = $3 убыток
- Это очень узкий стоп → только для снайперских входов

Пример с 10x leverage:
- Позиция: $3,000
- Стоп: 0.1% от цены = $3 убыток
- Более реалистичный стоп
```

## Выход по Фазам

```
TP1 (30% позиции): R:R 1:1 → забрать прибыль, стоп на безубыток
TP2 (30% позиции): R:R 1:2 → забрать, трейл на TP1
TP3 (40% позиции): R:R 1:3+ → трейл по структуре
```

## Реалистичные Ожидания

| Метрика | Консервативно | Агрессивно |
|---------|--------------|------------|
| Win Rate | 55-65% | 50-55% |
| R:R средний | 1:2 | 1:3+ |
| Сделок в день | 2-3 | 3-5 |
| Дневная доходность | 1-3% | 3-7% |
| Месячная (компаунд) | 20-60% | 60-200% |
| Макс. просадка | 5-10% | 10-20% |

---

# ЧАСТЬ IV: ТЕХНИЧЕСКИЙ СТЕК 2025

## Обзор Архитектуры v2

```
┌─────────────────────────────────────────────────────────┐
│                      HEAN v2 STACK                       │
├─────────────────────────────────────────────────────────┤
│  📱 SwiftUI App ←WebSocket→ FastAPI Gateway             │
│  🌐 Web Dashboard (React + WASM + Lightweight Charts)    │
│  🤖 Telegram Bot (уведомления + команды)                 │
├─────────────────────────────────────────────────────────┤
│  ⚡ Redis (In-Memory Bus)                                │
│     Streams · Pub/Sub · TimeSeries                       │
├──────┬──────┬──────────┬──────────────────────────────┤
│ Coll-│ Phys-│ Brain    │ Risk Guardian                  │
│ ector│ ics  │ (Claude  │ (железные правила              │
│(Py)  │(Rust │  + ONNX) │  + авто-стоп)                  │
│      │ +Py) │          │                                │
├──────┴──────┴──────────┴──────────────────────────────┤
│  💾 DuckDB + Parquet (аналитика + архив)                 │
│  🦀 hean_core (Rust через PyO3 — горячие пути)          │
│  🐳 Docker Compose (оркестрация всего)                   │
└─────────────────────────────────────────────────────────┘
```

---

## 🐍 БЭКЕНД: Python 3.12+

### Почему Python 3.12+

- **TaskGroup** для параллельного запуска сборщиков данных
- **ExceptionGroup** для обработки множественных ошибок WebSocket
- Самый быстрый CPython за всю историю (3.12 на 5-15% быстрее 3.11)
- Гигантская экосистема AI/ML, все биржевые API

### Ядро (Core)

| Библиотека | Версия | Назначение |
|-----------|--------|------------|
| Python | 3.12+ | Runtime — TaskGroup, улучшенный GIL |
| uvloop | 0.21+ | Event loop — 2-4x быстрее стандартного asyncio |
| asyncio | stdlib | Нативная async/await — основа всего |

### Веб-фреймворк и API

| Библиотека | Назначение |
|-----------|------------|
| FastAPI 0.115+ | Async REST + WebSocket сервер |
| uvicorn 0.34+ | ASGI-сервер на uvloop |
| websockets 14+ | WebSocket клиент для бирж |
| pydantic 2.10+ | Валидация (Rust ядро = 50x быстрее v1) |
| orjson 3.10+ | JSON на Rust — 10x быстрее стандартного |

### Подключение к биржам

| Библиотека | Назначение |
|-----------|------------|
| ccxt 4.4+ | 100+ бирж одним интерфейсом |
| python-binance | Binance WebSocket стримы |
| pybit | Bybit WebSocket + REST |

### Обработка данных

| Библиотека | Назначение |
|-----------|------------|
| polars 1.20+ | DataFrame на Rust — 10-50x быстрее Pandas |
| numpy 2.2+ | Вычисления (C/Fortran ядро) |
| scipy 1.15+ | Энтропия, статистика, фильтры |
| duckdb 1.2+ | Аналитическая OLAP БД — быстрее SQLite в 100x |

### AI / Machine Learning

| Библиотека | Назначение |
|-----------|------------|
| anthropic 0.43+ | Claude API — стратегический мозг |
| onnxruntime 1.20+ | Тактический мозг — inference <5мс |
| scikit-learn 1.6+ | Кластеризация участников |
| ta-lib | Технические индикаторы (C ядро) |
| hmmlearn | Hidden Markov Models для режимов рынка |

### Мониторинг и DevOps

| Библиотека | Назначение |
|-----------|------------|
| loguru 0.7+ | Логирование с цветами и ротацией |
| prometheus-client | Метрики для мониторинга |
| httpx 0.28+ | HTTP клиент с retry и timeout |

---

## 📱 ФРОНТЕНД: Swift 6 + SwiftUI

### Почему Swift 6 + SwiftUI

- **Swift 6 strict concurrency** — гарантия отсутствия data races
- **@Observable** (iOS 17+) — автоматическое обновление UI без ObservableObject
- **Swift Charts** — встроенные графики Apple (профессиональные, нативные)
- **async/await** — нативная асинхронность, WebSocket без callback hell
- **SwiftNIO** — высокопроизводительный networking (как Netty для Java)

### Архитектура iOS App

```
HEAN/
├── App/
│   └── HEANApp.swift
├── Core/
│   ├── Network/
│   │   ├── WebSocketManager.swift     # Подключение к серверу
│   │   └── APIClient.swift            # REST запросы
│   ├── Models/
│   │   ├── MarketState.swift          # @Observable модель рынка
│   │   ├── Signal.swift               # Торговый сигнал
│   │   └── Thermodynamics.swift       # T, S, фаза
│   └── Services/
│       ├── TradingEngine.swift        # Управление позициями
│       └── NotificationService.swift  # Push + звуки
├── Features/
│   ├── Dashboard/
│   │   └── DashboardView.swift
│   ├── Brain/
│   │   └── BrainView.swift            # Визуализация мышления
│   ├── GravityMap/
│   │   └── GravityMapView.swift       # Карта ликвидаций
│   ├── Players/
│   │   └── PlayersView.swift          # Рентген участников
│   ├── TemporalStack/
│   │   └── TemporalStackView.swift    # 5 уровней времени
│   └── Trade/
│       └── TradeView.swift            # Активная сделка
└── Resources/
    ├── Assets.xcassets
    └── Sounds/
```

### Пример: Подключение к серверу

```swift
@Observable
class MarketState {
    var temperature: Double = 0
    var entropy: Double = 0
    var phase: MarketPhase = .ice
    var signal: TradingSignal?
    var players: [MarketPlayer] = []
    
    private var webSocket: URLSessionWebSocketTask?
    
    func connect() async {
        let url = URL(string: "ws://your-server:8000/ws")!
        webSocket = URLSession.shared.webSocketTask(with: url)
        webSocket?.resume()
        
        while true {
            guard let message = try? await webSocket?.receive() else { break }
            if case .string(let text) = message,
               let data = text.data(using: .utf8) {
                let update = try? JSONDecoder().decode(MarketUpdate.self, from: data)
                await MainActor.run {
                    self.temperature = update?.temperature ?? self.temperature
                    self.entropy = update?.entropy ?? self.entropy
                    self.signal = update?.signal
                }
            }
        }
    }
}
```

---

## 💾 ХРАНЕНИЕ ДАННЫХ

### DuckDB + Parquet

```
Горячие данные (сегодня):
  DuckDB in-memory → мгновенные запросы

Тёплые данные (неделя):
  DuckDB on-disk → быстрые запросы

Холодные данные (архив):
  Parquet файлы → компрессия, колоночный формат
  
Данные реального времени (v2):
  Redis → <1мс доступ → микросервисы
```

### Пример запросов DuckDB

```sql
-- Средняя температура за последний час
SELECT AVG(temperature) FROM market_state 
WHERE timestamp > now() - INTERVAL '1 hour';

-- Все аномалии с severity > 0.8 за сегодня
SELECT * FROM anomalies 
WHERE severity > 0.8 AND date = current_date
ORDER BY timestamp DESC;

-- Win rate по стратегиям
SELECT strategy, 
       COUNT(*) as trades,
       AVG(CASE WHEN pnl > 0 THEN 1.0 ELSE 0.0 END) as win_rate
FROM trades GROUP BY strategy;
```

---

## 🔗 ПРОТОКОЛ ОБЩЕНИЯ APP ↔ SERVER

### WebSocket (реальное время)

```json
// Server → App (каждые 100мс)
{
  "type": "market_update",
  "data": {
    "price": 94247.50,
    "temperature": 847,
    "entropy": 0.34,
    "phase": "ice_to_water",
    "players": {
      "mm_activity": 0.89,
      "institutional_flow": -12000000,
      "retail_sentiment": 0.78
    },
    "signal": {
      "direction": "SHORT",
      "confidence": 0.82,
      "entry": 94200,
      "stop": 94800,
      "tp1": 93600,
      "tp2": 93200,
      "rr": 4.2
    }
  }
}
```

### REST API (команды)

```
POST /api/trade/execute    — исполнить сделку
POST /api/trade/close      — закрыть позицию
GET  /api/status           — статус системы
GET  /api/history          — история сделок
POST /api/settings         — настройки рисков
GET  /api/brain/thoughts   — лог мышления AI
```

---

# ЧАСТЬ V: 5 УСИЛИТЕЛЕЙ v2 (POWER UPGRADES)

## 🦀 Усилитель 1: Rust-ядро через PyO3

**Самый мощный апгрейд.** Не переписываем всё на Rust — выносим только горячие пути.

### Что выносим в Rust

```
┌─────────────────────────────────────────┐
│              hean_core (Rust)            │
├─────────────────────────────────────────┤
│  • Парсер ордербука (тысячи updates/с)  │
│  • Вычисление T и S (float-heavy)       │
│  • Кластеризатор участников (real-time)  │
│  • Matching engine для бэктеста          │
│  • Encoder/decoder WebSocket             │
└─────────────────────────────────────────┘
```

### Пример

```python
# Python вызывает Rust через PyO3
import hean_core

# 100x быстрее чем чистый Python
temp = hean_core.market_temperature(orderbook_data)
entropy = hean_core.market_entropy(orderbook_data)
clusters = hean_core.classify_participants(trade_flow)
```

### Сборка

```bash
pip install maturin
cd hean_core/
maturin develop  # компилирует Rust → Python модуль
```

**Тренд 2025:** Polars, Pydantic v2, orjson, cryptography, blake3 — все используют Rust через PyO3.

**Результат:** 100x ускорение горячих путей + безопасность памяти Rust + простота Python.

---

## ⚡ Усилитель 2: Redis (In-Memory Data Bus)

### Зачем

БЕЗ Redis: монолит, всё в одном процессе.
С Redis: микросервисы, каждый модуль независим.

```
WebSocket бирж → Redis Streams → [множество потребителей]
                     ├── Physics Engine (T, S)
                     ├── Participant Analyzer
                     ├── Brain Service (Claude + ONNX)
                     ├── Risk Guardian
                     └── App WebSocket (iPhone)
```

### Пример

```python
import redis.asyncio as redis

r = redis.Redis()

# Публикация данных
await r.xadd("market:btcusdt", {
    "price": "94247.50",
    "temperature": "847",
    "entropy": "0.34"
})

# Подписка (в другом сервисе)
async for message in r.xread({"market:btcusdt": "$"}, block=0):
    process(message)
```

### Преимущества

| Аспект | Монолит | Redis Bus |
|--------|---------|-----------|
| Отказоустойчивость | Упал один → упало всё | Упал один → остальные работают |
| Скорость | In-process | <1мс между сервисами |
| Масштабирование | Вертикальное | Горизонтальное |
| Добавить модуль | Рефакторинг | Новый потребитель |

**Real-world:** HFT системы достигают P50=2.1ms, P95=2.8ms для 210 stocks, 12,600 ticks/min.

---

## 🐳 Усилитель 3: Docker Compose (Оркестрация)

### docker-compose.yml

```yaml
version: '3.8'
services:
  redis:
    image: redis/redis-stack
    ports: ["6379:6379"]
    
  collector:
    build: ./services/collector
    depends_on: [redis]
    restart: always
    environment:
      - BYBIT_WS=wss://stream.bybit.com/v5/public/linear
      
  physics:
    build: ./services/physics
    depends_on: [redis]
    restart: always
    
  brain:
    build: ./services/brain
    depends_on: [redis]
    restart: always
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      
  risk:
    build: ./services/risk
    depends_on: [redis]
    restart: always
    
  api:
    build: ./services/api
    ports: ["8000:8000"]
    depends_on: [redis]
    restart: always
```

**Одна команда:** `docker compose up -d` — вся система работает.
**Упал сервис?** Авто-перезапуск за 2 секунды.

---

## 🔥 Усилитель 4: WASM Web Dashboard

Дополнительная визуализация помимо iPhone:

```
📱 iPhone (SwiftUI) — основной интерфейс
🌐 Web Dashboard — любое устройство
    ├── Lightweight Charts (TradingView open-source)
    ├── D3.js графики (температура, энтропия)
    └── Rust-код в браузере (WASM)
🤖 Telegram Bot — уведомления + быстрые команды
```

**Lightweight Charts:** профессиональные финансовые графики, бесплатная open-source библиотека от TradingView.

---

## 🧬 Усилитель 5: ONNX Runtime (нейросети на скорости C++)

### Workflow

```
Обучение (раз в неделю):
  Python + PyTorch → тренировка на исторических данных
  → export в ONNX формат

Inference (каждые 100мс):
  ONNX Runtime (C++) → классификация за <5мс
  → "опасно / безопасно / вход"
```

### Пример

```python
import onnxruntime as ort

session = ort.InferenceSession("hean_classifier.onnx")
result = session.run(None, {
    "orderbook_imbalance": np.array([[0.72, -2.4, 0.34]]),
    "delta_flow": np.array([[-2400000]]),
    "temperature": np.array([[847]]),
    "entropy": np.array([[0.34]])
})
# → [0.87] = 87% вероятность каскада за <5мс
```

---

# ЧАСТЬ VI: ПОДКЛЮЧЕНИЕ К БИРЖАМ

## Вариант 1: CEX через CCXT + API ключи

```
Пользователь → создаёт API ключи на бирже → вводит в HEAN
HEAN Backend (Python + CCXT) → торгует через API

Поддержка: Bybit, Binance, OKX, Bitget и 100+ бирж
```

```python
import ccxt.async_support as ccxt

exchange = ccxt.bybit({
    'apiKey': encrypted_key,
    'secret': encrypted_secret,
    'options': {'defaultType': 'linear'}
})

# Один и тот же код для ЛЮБОЙ биржи
order = await exchange.create_order('BTC/USDT:USDT', 'limit', 'sell', 0.01, 94200)
```

## Вариант 2: DEX через WalletConnect (без API ключей)

```
HEAN (SwiftUI) → WalletConnect → кошелёк пользователя → Hyperliquid

Никаких ключей. Пользователь сканирует QR или подтверждает в кошельке.
```

**Hyperliquid:** перпетуальные фьючерсы до 50x, скорость как CEX, авторизация через кошелёк.

## Рекомендуемая схема

```
HEAN App (SwiftUI)
  │
  ├── CEX модуль
  │     └── API ключи → Backend (Python/CCXT) → Биржа
  │
  └── DEX модуль
        └── WalletConnect → Кошелёк → Hyperliquid
```

---

# ЧАСТЬ VII: ПОЛНЫЙ REQUIREMENTS.TXT

```txt
# ═══════════ ЯДРО ═══════════
python>=3.12
uvloop>=0.21
asyncio

# ═══════════ ВЕБ-ФРЕЙМВОРК ═══════════
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=14.0
pydantic>=2.10
orjson>=3.10

# ═══════════ БИРЖИ ═══════════
ccxt>=4.4
python-binance>=1.0
pybit>=5.9

# ═══════════ ДАННЫЕ ═══════════
polars>=1.20
numpy>=2.2
scipy>=1.15
duckdb>=1.2
pyarrow>=18.0

# ═══════════ AI / ML ═══════════
anthropic>=0.43
onnxruntime>=1.20
scikit-learn>=1.6
hmmlearn>=0.3
TA-Lib

# ═══════════ REDIS (v2) ═══════════
redis[hiredis]>=5.2

# ═══════════ RUST ЯДРО (v2) ═══════════
maturin>=1.8

# ═══════════ TELEGRAM (v2) ═══════════
python-telegram-bot>=22

# ═══════════ УТИЛИТЫ ═══════════
loguru>=0.7
httpx>=0.28
python-dotenv>=1.0
cryptography>=44.0
prometheus-client>=0.21
```

---

# ЧАСТЬ VIII: ФАЙЛОВАЯ СТРУКТУРА ПРОЕКТА

```
hean/
├── docker-compose.yml              # Оркестрация всех сервисов
├── .env                            # API ключи (зашифрованные)
├── requirements.txt
│
├── hean_core/                      # 🦀 Rust ядро (PyO3)
│   ├── Cargo.toml
│   ├── pyproject.toml
│   └── src/
│       ├── lib.rs                  # Точка входа
│       ├── orderbook.rs            # Парсер ордербука
│       ├── thermodynamics.rs       # T, S, фазы
│       ├── clustering.rs           # Кластеризация участников
│       └── backtest.rs             # Matching engine
│
├── services/                       # 🐳 Микросервисы
│   ├── collector/                  # Сбор данных с бирж
│   │   ├── Dockerfile
│   │   ├── main.py
│   │   ├── bybit_ws.py
│   │   ├── binance_ws.py
│   │   └── coinglass.py
│   │
│   ├── physics/                    # Термодинамика
│   │   ├── Dockerfile
│   │   ├── main.py
│   │   ├── temperature.py
│   │   ├── entropy.py
│   │   └── phase_detector.py
│   │
│   ├── brain/                      # AI мозг
│   │   ├── Dockerfile
│   │   ├── main.py
│   │   ├── claude_client.py        # Стратегический мозг
│   │   ├── onnx_classifier.py      # Тактические рефлексы
│   │   ├── participant_analyzer.py # Рентген участников
│   │   └── models/
│   │       └── hean_classifier.onnx
│   │
│   ├── risk/                       # Управление рисками
│   │   ├── Dockerfile
│   │   ├── main.py
│   │   ├── iron_rules.py           # Железные правила
│   │   ├── position_sizer.py
│   │   └── stop_manager.py
│   │
│   └── api/                        # Gateway для iPhone
│       ├── Dockerfile
│       ├── main.py
│       ├── ws_handler.py           # WebSocket для app
│       └── rest_routes.py          # REST API
│
├── data/                           # 💾 Хранение
│   ├── duckdb/
│   │   └── hean.db
│   └── parquet/
│       └── archive/
│
├── web/                            # 🌐 Web Dashboard (v2)
│   ├── package.json
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── Chart.tsx           # Lightweight Charts
│   │   │   ├── Temperature.tsx
│   │   │   └── Entropy.tsx
│   │   └── wasm/                   # Rust → WASM
│   └── public/
│
├── ios/                            # 📱 iPhone App
│   └── HEAN/
│       ├── HEANApp.swift
│       ├── Core/
│       │   ├── Network/
│       │   ├── Models/
│       │   └── Services/
│       └── Features/
│           ├── Dashboard/
│           ├── Brain/
│           ├── GravityMap/
│           ├── Players/
│           ├── TemporalStack/
│           └── Trade/
│
├── notebooks/                      # 📊 Исследования
│   ├── thermodynamics_research.ipynb
│   ├── clustering_experiments.ipynb
│   └── backtest_results.ipynb
│
├── tests/
│   ├── test_physics.py
│   ├── test_risk.py
│   └── test_signals.py
│
└── scripts/
    ├── setup.sh                    # Установка с нуля
    ├── train_onnx.py               # Обучение ONNX модели
    └── backtest.py                 # Бэктестинг
```

---

# ЧАСТЬ IX: ДОРОЖНАЯ КАРТА

```
╔══════════════════════════════════════════════════════════╗
║                    HEAN ROADMAP                          ║
╠══════════════════════════════════════════════════════════╣
║                                                          ║
║  ФАЗА 1: ГЛАЗА + МОЗГ (Недели 1-2)                     ║
║  ├── WebSocket подключение к Bybit                      ║
║  ├── Сбор ордербука, сделок, ликвидаций                 ║
║  ├── Claude API интеграция                              ║
║  ├── Базовый FastAPI сервер                             ║
║  └── DuckDB хранение                                    ║
║                                                          ║
║  ФАЗА 2: ФИЗИКА (Недели 3-4)                            ║
║  ├── Температура рынка (T)                               ║
║  ├── Энтропия (S)                                        ║
║  ├── Фазовые переходы                                    ║
║  └── Первые сигналы                                      ║
║                                                          ║
║  ФАЗА 3: РЕНТГЕН (Недели 5-6)                           ║
║  ├── Кластеризация участников                            ║
║  ├── Детектор стоп-хантинга                              ║
║  └── Межрыночные импульсы                                ║
║                                                          ║
║  ФАЗА 4: iPHONE APP (Недели 3-8, параллельно)           ║
║  ├── Dashboard, Brain, GravityMap                        ║
║  ├── WebSocket подключение к серверу                     ║
║  └── Push-уведомления                                    ║
║                                                          ║
║  ФАЗА 5: ПЕРВАЯ КРОВЬ (Недели 7-8)                      ║
║  ├── Paper trading (тестовые деньги)                     ║
║  ├── Подключение CCXT + Bybit                           ║
║  └── Risk management модуль                              ║
║                                                          ║
║  ФАЗА 6: ДЕМОН (Недели 9-12)                            ║
║  ├── Темпоральный стек                                   ║
║  ├── Мета-игра                                           ║
║  └── Самообучающиеся агенты                              ║
║                                                          ║
║  ═══════ POWER UPGRADES (v2) ═══════                     ║
║                                                          ║
║  ФАЗА 7: + REDIS (+2 недели)                            ║
║  ├── Развязка на микросервисы                            ║
║  └── Redis Streams / Pub/Sub                             ║
║                                                          ║
║  ФАЗА 8: + DOCKER (+3 недели)                           ║
║  ├── Контейнеризация всех сервисов                      ║
║  └── docker-compose up -d                                ║
║                                                          ║
║  ФАЗА 9: + RUST ЯДРО (+1 месяц)                         ║
║  ├── hean_core на Rust через PyO3                        ║
║  └── 100x ускорение горячих путей                        ║
║                                                          ║
║  ФАЗА 10: + ONNX (+2 месяца)                            ║
║  ├── Тактический мозг на ONNX                            ║
║  └── Двойной мозг: Claude + ONNX                         ║
║                                                          ║
║  ФАЗА 11: + WEB DASHBOARD (+2 месяца)                   ║
║  ├── React + Lightweight Charts                          ║
║  ├── WASM модули                                         ║
║  └── Telegram Bot                                        ║
║                                                          ║
╚══════════════════════════════════════════════════════════╝
```

---

# ЧАСТЬ X: СРАВНЕНИЕ v1 vs v2

| Аспект | HEAN v1 | HEAN v2 | Прирост |
|--------|---------|---------|---------|
| Архитектура | Монолит | Микросервисы (Redis) | Отказоустойчивость |
| Горячие пути | Python | Rust через PyO3 | 100x скорость |
| Данные RT | In-process | Redis <1мс | Развязка сервисов |
| Мозг | Claude API | Claude + ONNX | <5мс рефлексы |
| Деплой | Ручной | Docker Compose | 1 команда |
| Визуализация | iPhone | iPhone + Web + Telegram | Везде |
| Хранение | DuckDB | Redis (hot) + DuckDB (cold) | Оптимально |

---

# ЧАСТЬ XI: НЕОБХОДИМЫЕ АККАУНТЫ И КЛЮЧИ

| Сервис | Что нужно | Стоимость |
|--------|-----------|-----------|
| Bybit | Аккаунт + API ключи | Бесплатно |
| Anthropic | API ключ для Claude | ~$5-20/мес |
| Coinglass | API ключ (опционально) | $0-30/мес |
| Telegram | Bot token (через @BotFather) | Бесплатно |
| GitHub | Репозиторий для кода | Бесплатно |

---

# ЧАСТЬ XII: ЖЕЛЕЗО И ДЕПЛОЙ

## Рекомендуемый вариант: Mac Mini

```
Mac Mini M2/M4 (~$500-800)
├── 8-16GB RAM (16GB для v2 с Redis)
├── 256GB+ SSD
├── Подключение по КАБЕЛЮ (не Wi-Fi!)
├── UPS (бесперебойник) — обязательно
└── Работает 24/7, тихо, энергоэффективно

Мощнее любого VPS за $20/мес. Купил один раз — нет ежемесячных платежей.
```

## Установка с нуля

```bash
# 1. Python бэкенд (5 минут)
brew install python@3.12 redis
pip install -r requirements.txt

# 2. Rust ядро (v2)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
pip install maturin
cd hean_core && maturin develop

# 3. Docker (v2)
brew install --cask docker
docker compose up -d

# 4. iOS проект (Xcode)
open ios/HEAN.xcodeproj
```

---

# ИТОГ

HEAN — это не просто торговый бот. Это **Демон Лапласа** для финансового рынка:

1. **ГЛАЗА** — видит ВСЁ (ордербук, ликвидации, whale, сентимент)
2. **ФИЗИКА** — измеряет T, S, фазовые переходы
3. **РЕНТГЕН** — знает кто есть кто на рынке
4. **ДЕМОН** — предсказывает неизбежное движение
5. **РЕФЛЕКСЫ** — реагирует за <5мс (ONNX)
6. **СТРАТЕГ** — думает как гений (Claude)
7. **БРОНЯ** — железные правила риска
8. **СКОРОСТЬ** — Rust ядро, 100x быстрее
9. **НАДЁЖНОСТЬ** — Redis + Docker, автовосстановление
10. **ВЕЗДЕ** — iPhone, Web, Telegram

> *Прибыль ≤ Температура × Информация*
> 
> HEAN максимизирует информацию. Рынок отдаёт деньги.

---

*Документ v2.0 | Февраль 2026 | HEAN Project*
