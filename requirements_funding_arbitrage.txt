# Multi-Exchange Funding Arbitrage Dependencies
# Install with: pip install -r requirements_funding_arbitrage.txt --break-system-packages

# Core dependencies (should already be installed)
aiohttp>=3.9.0  # Async HTTP client
asyncio  # Already in Python 3.11+

# Optional: For better performance
orjson>=3.9.0  # Fast JSON parsing (optional but recommended)

# Note: These are the ONLY additional dependencies needed!
# The funding arbitrage module reuses existing HEAN infrastructure:
# - hean.core.bus (EventBus)
# - hean.core.types (Signal, Event)
# - hean.strategies.base (BaseStrategy)
# - hean.logging (get_logger)
