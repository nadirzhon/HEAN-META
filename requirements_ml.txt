# ============================================================================
# HEAN ML Stack Dependencies - Phase 1-3
# ============================================================================

# Core ML Libraries
# ============================================================================
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Technical Analysis
# ============================================================================
# TA-Lib (requires system library: brew install ta-lib or apt-get install libta-lib)
TA-Lib>=0.4.28
# Alternative if TA-Lib fails to compile:
# ta-lib-python>=0.4.28  # Pure Python fallback (slower)

# Machine Learning - Ensemble Models (Phase 1)
# ============================================================================
lightgbm>=4.1.0          # Fast gradient boosting
xgboost>=2.0.0           # XGBoost for ensemble
catboost>=1.2.0          # CatBoost for ensemble
scikit-learn>=1.3.0      # Utilities, preprocessing, metrics

# Deep Learning (Phase 2)
# ============================================================================
torch>=2.1.0             # PyTorch for neural networks
pytorch-forecasting>=1.0.0  # Temporal Fusion Transformer, N-BEATS

# Reinforcement Learning (Phase 2)
# ============================================================================
stable-baselines3>=2.2.0  # PPO, A2C, SAC algorithms
gymnasium>=0.29.0        # RL environment (OpenAI Gym successor)
tensorboard>=2.15.0      # Training visualization

# Backtesting & Optimization (Phase 1)
# ============================================================================
vectorbt>=0.26.0         # Fast vectorized backtesting
optuna>=3.5.0            # Hyperparameter optimization
bayesian-optimization>=1.4.3  # Bayesian optimization

# Alternative Data (Phase 2)
# ============================================================================
# Sentiment Analysis
transformers>=4.35.0     # FinBERT, sentiment models
sentencepiece>=0.1.99    # Tokenization
torch>=2.1.0             # Required for transformers

# On-Chain & Market Data
ccxt>=4.2.0              # Exchange APIs (multi-exchange)
requests>=2.31.0         # HTTP requests for APIs
beautifulsoup4>=4.12.0   # Web scraping (optional)

# Infrastructure (Phase 1-3)
# ============================================================================
redis>=5.0.0             # Caching & Redis Streams (already in main requirements)
loguru>=0.7.2            # Better logging
pyyaml>=6.0.1            # Config files

# Event Streaming (Phase 3)
# ============================================================================
aiokafka>=0.10.0         # Async Kafka client (optional, for production scale)
# Note: Kafka is optional - Redis Streams works for most use cases

# Analytics & Storage
# ============================================================================
duckdb>=0.9.0            # Fast analytical queries
pyarrow>=14.0.0          # Columnar storage
polars>=0.20.0           # Fast DataFrame library (alternative to pandas)

# Visualization & Monitoring (Phase 1)
# ============================================================================
matplotlib>=3.8.0        # Basic plots
seaborn>=0.13.0          # Statistical plots
plotly>=5.18.0           # Interactive plots
prometheus-client>=0.19.0  # Already in main requirements

# Utilities
# ============================================================================
joblib>=1.3.0            # Model serialization, parallel processing
tqdm>=4.66.0             # Progress bars
python-dateutil>=2.8.2   # Date utilities

# Statistical Analysis
# ============================================================================
statsmodels>=0.14.0      # Statistical models, time series
arch>=6.2.0              # GARCH models for volatility

# Quantum Computing (Phase 3 - Experimental)
# ============================================================================
# qiskit>=0.45.0         # IBM Quantum (uncomment if needed)
# qiskit-optimization>=0.6.0  # Portfolio optimization

# Development & Testing
# ============================================================================
pytest>=7.4.0
pytest-asyncio>=0.21.0
mypy>=1.7.0
black>=23.12.0
isort>=5.13.0

# Optional: GPU Acceleration
# ============================================================================
# Install GPU versions manually if needed:
# - PyTorch: https://pytorch.org/get-started/locally/
# - XGBoost GPU: pip install xgboost[gpu]
# - LightGBM GPU: pip install lightgbm --install-option=--gpu

# Notes:
# ============================================================================
# 1. TA-Lib Installation (REQUIRED for Phase 1):
#    - macOS: brew install ta-lib && pip install TA-Lib
#    - Ubuntu: apt-get install libta-lib-dev && pip install TA-Lib
#    - Windows: Download wheel from https://github.com/cgohlke/talib-build/releases
#
# 2. For M1/M2 Macs:
#    - Use conda: conda install -c conda-forge ta-lib
#
# 3. Docker installation:
#    - See Dockerfile.ml for complete setup
#
# 4. Minimal install (no TA-Lib, no RL, no DL):
#    pip install numpy pandas scikit-learn lightgbm xgboost optuna vectorbt
