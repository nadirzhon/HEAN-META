# ML Price Predictor Dependencies
# Install with: pip install -r requirements_ml_predictor.txt --break-system-packages

# Deep Learning
tensorflow>=2.13.0  # LSTM neural network
# OR use tensorflow-cpu if no GPU:
# tensorflow-cpu>=2.13.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0  # For preprocessing, scaling

# Technical indicators (optional - can use pandas-ta or ta-lib)
# pandas-ta>=0.3.14b  # Technical analysis library
# ta-lib  # Requires separate C library installation

# Visualization (optional for training monitoring)
matplotlib>=3.7.0  # For plotting training progress
seaborn>=0.12.0  # For better plots

# Already included in HEAN (no need to reinstall):
# - asyncio (Python 3.11+)
# - logging (Python standard library)
# - datetime (Python standard library)
