# 🚀 Запуск HEAN одной командой

## ✨ Быстрый старт

Теперь весь проект HEAN запускается **одной командой**!

```bash
./start_hean_complete.sh
```

Скрипт автоматически:
- ✅ Проверит все файлы конфигурации
- ✅ Проверит API ключи
- ✅ Остановит старые контейнеры
- ✅ Соберет Docker образы
- ✅ Запустит все сервисы в правильном порядке
- ✅ Проверит здоровье каждого сервиса
- ✅ Покажет статус и информацию о доступе

---

## 📋 Все доступные команды

### Запуск всех сервисов
```bash
./start_hean_complete.sh
```

### Проверка статуса
```bash
./status_hean.sh
```

### Остановка всех сервисов
```bash
./stop_hean.sh
```

### Просмотр логов
```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f symbiont-testnet
docker compose logs -f api
docker compose logs -f redis
```

### Перезапуск
```bash
docker compose restart
```

---

## 🔧 Что запускается

Скрипт запускает следующие сервисы:

1. **Redis** - База данных для обмена состоянием
   - Порт: 6379

2. **Backend API** - FastAPI сервер
   - Порт: 8000
   - Документация: http://localhost:8000/docs

3. **Symbiont Trading Bot** - Торговый бот на Bybit Testnet
   - Работает с реальными данными Bybit

4. **Collector** - Сбор рыночных данных через WebSocket

5. **Physics** - Расчеты термодинамики рынка

6. **Brain** - AI принятие решений (Claude)

7. **Risk** - Управление рисками

---

## 📱 Подключение iOS приложения

После запуска backend, iOS приложение может подключиться к:

```
http://localhost:8000
```

Или если запущено на другом компьютере:
```
http://YOUR_IP:8000
```

---

## ⚠️ Первый запуск

При первом запуске вам нужно **один раз** настроить API ключи:

1. Откройте файл `backend.env`
2. Добавьте ваши ключи от Bybit:
   ```bash
   BYBIT_API_KEY=ваш_ключ
   BYBIT_API_SECRET=ваш_секрет
   ```
3. Сохраните файл
4. Запустите `./start_hean_complete.sh`

---

## 🎯 Автоматический запуск по расписанию

Хотите чтобы система запускалась автоматически каждый день?

Я создам для вас **автоматический shortcut** который будет:
- Запускать HEAN в нужное время (например, каждое утро)
- Проверять статус
- Отправлять уведомления

Скажите когда вам удобно запускать - создам автоматизацию!

---

## 🐛 Решение проблем

### Docker не запускается
```bash
# Проверьте что Docker Desktop запущен
docker info
```

### Порт занят
```bash
# Освободите порт 8000 или измените в docker-compose.yml
lsof -ti:8000 | xargs kill
```

### API ключи не работают
```bash
# Получите новые testnet ключи:
# https://testnet.bybit.com/
```

### Контейнер не запускается
```bash
# Посмотрите логи конкретного контейнера
docker compose logs symbiont-testnet
```

---

## 💡 Полезные советы

- **Всегда запускайте** `./status_hean.sh` чтобы проверить что все работает
- **Смотрите логи** если что-то не так: `docker compose logs -f`
- **Не забывайте останавливать** сервисы когда не нужны: `./stop_hean.sh`

---

## ✅ Готово!

Теперь вам **не нужно каждый день** вручную запускать множество команд!

**Просто запустите:**
```bash
./start_hean_complete.sh
```

**И всё работает! 🎉**
