# 🚀 Запуск нового веб-интерфейса

## Быстрый старт (1 команда)

```bash
docker-compose up -d --build ui
# или полный стек: docker-compose up -d --build
```

Интерфейс: **http://localhost:3000**

## Проверка

```bash
docker ps | grep hean-ui
docker logs hean-ui
curl http://localhost:3000/
```

## Остановка / запуск

```bash
docker-compose stop ui      # остановить
docker-compose start ui     # запустить
docker-compose restart ui   # перезапустить
docker-compose rm -f ui     # удалить контейнер
```

## Режим разработки (hot reload)

```bash
docker-compose --profile dev up -d --build api ui-dev
```

UI-dev слушает на **http://localhost:5173** и монтирует исходники `apps/ui`.

## Ручная сборка

```bash
cd apps/ui
docker build \
  --build-arg VITE_API_BASE=http://localhost:8000 \
  --build-arg VITE_WS_URL=ws://localhost:8000/ws \
  -t hean-ui .
docker run -d -p 3000:80 --name hean-ui hean-ui
```

## Если изменения не видно

```bash
docker-compose build --no-cache ui
docker-compose up -d ui
```

Очистите кэш браузера (Cmd/Ctrl + Shift + R).

## Порт 3000 занят?

В `docker-compose.yml` замените строку:

```yaml
ports:
  - "8080:80"
```

## Справка

- Подробности: `WEB_DOCKER_SETUP.md`
- Общая документация: `README.md`
