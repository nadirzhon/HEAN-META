# 🚀 ЗАПУСК ПРОЕКТА HEAN ПРЯМО СЕЙЧАС

## ⚡ Быстрый запуск (3 минуты)

### Вариант 1: Через Docker (Рекомендуется)

Откройте **Терминал** на вашем Mac и выполните:

```bash
cd ~/Desktop/HEAN
./start_hean_complete.sh
```

**Скрипт автоматически:**
- ✅ Проверит все конфигурации
- ✅ Соберет Docker образы
- ✅ Запустит все сервисы
- ✅ Покажет статус

---

### Вариант 2: Без Docker (Быстрый тест)

Если Docker не запускается, протестируйте подключение:

```bash
cd ~/Desktop/HEAN

# 1. Проверка подключения к Bybit
python3 test_bybit_connection_simple.py

# 2. Если подключение OK, запустите бота
python3 live_testnet_real.py
```

---

## 📊 Что вы увидите

### При успешном запуске:

```
✅ REAL DATA RECEIVED:
   Symbol: BTCUSDT
   Price: $50,234.12
   24h Change: +2.34%

✅ RECEIVED 10 REAL KLINES:
   Candle  1: 13:40:00 | O: $50,123 | C: $50,234 | +0.22% | 🟢
   Candle  2: 13:45:00 | O: $50,234 | C: $50,345 | +0.22% | 🟢

🤖 SYMBIONT X Trading Bot - ACTIVE
   Strategy: Impulse Engine
   Capital: $10,000
   Risk Level: LOW
```

---

## 🎯 Проверка результатов

### 1. Проверьте логи бота:

```bash
cd ~/Desktop/HEAN
docker compose logs -f symbiont-testnet
```

Или без Docker:
```bash
tail -f logs/trading_*.log
```

### 2. Проверьте API:

Откройте в браузере: **http://localhost:8000/docs**

### 3. Проверьте сделки на Bybit:

Зайдите на https://testnet.bybit.com/ и посмотрите:
- **Orders** - активные ордера
- **Positions** - открытые позиции
- **Trade History** - история сделок

---

## 🐛 Если что-то не работает

### Проблема: Docker не запускается

```bash
# Проверьте Docker Desktop
open -a "Docker Desktop"

# Подождите пока Docker запустится (значок в трее)
# Затем снова запустите:
./start_hean_complete.sh
```

### Проблема: Порт 8000 занят

```bash
# Освободите порт
lsof -ti:8000 | xargs kill -9

# Запустите снова
./start_hean_complete.sh
```

### Проблема: API ключи не работают

1. Зайдите на https://testnet.bybit.com/
2. API Management → Create New Key
3. Скопируйте новые ключи
4. Откройте `backend.env` и замените:
   ```
   BYBIT_API_KEY=новый_ключ
   BYBIT_API_SECRET=новый_секрет
   ```

### Проблема: "Module not found"

```bash
# Установите зависимости
cd ~/Desktop/HEAN
pip3 install -r requirements.txt
```

---

## 📱 Подключение iOS приложения

После запуска backend:

1. Откройте Xcode:
   ```bash
   cd ~/Desktop/HEAN/ios
   open HEAN.xcodeproj
   ```

2. В файле конфигурации измените URL на:
   ```
   http://localhost:8000
   ```

3. Запустите приложение на симуляторе или устройстве

---

## ✅ Контрольный список готовности

- [ ] Docker Desktop запущен (если используете Docker)
- [ ] API ключи Bybit testnet настроены в `backend.env`
- [ ] Порт 8000 свободен
- [ ] Python 3.10+ установлен
- [ ] Интернет соединение работает

---

## 🚀 НАЧНИТЕ ПРЯМО СЕЙЧАС!

Выполните в терминале:

```bash
cd ~/Desktop/HEAN
./start_hean_complete.sh
```

**И бот начнет торговать на Bybit testnet!** 📈💰

---

## 📞 Нужна помощь?

Если что-то не получается:
1. Скопируйте вывод ошибки
2. Отправьте мне - я помогу исправить
3. Или запустите: `./status_hean.sh` для диагностики
