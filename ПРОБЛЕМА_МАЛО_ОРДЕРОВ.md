# 🔍 ПРОБЛЕМА: За 24 часа только 2 ордера

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

- **Ордеров за 24 часа:** 2
- **Сигналов сгенерировано:** 0 (по данным диагностики)
- **Статус системы:** Running (через API), но диагностика показывает stopped

## 🔴 ОСНОВНЫЕ ПРИЧИНЫ

### 1. **PAPER_TRADE_ASSIST ВЫКЛЮЧЕН** ⚠️
   - **Проблема:** Фильтры слишком строгие для paper trading
   - **Решение:** Включить `PAPER_TRADE_ASSIST=true` в `.env`

### 2. **СИСТЕМА МОЖЕТ НЕ ПОЛУЧАТЬ ТИКИ**
   - **Проблема:** Synthetic price feed генерирует тики, но они могут не доходить до стратегий
   - **Решение:** Проверить логи на наличие TICK events

### 3. **СТРАТЕГИИ БЛОКИРУЮТСЯ ФИЛЬТРАМИ**
   - **Проблема:** Слишком строгие фильтры (spread, volatility, edge)
   - **Решение:** Включить `DEBUG_MODE=true` для ослабления фильтров

### 4. **РЕЖИМ РЫНКА НЕ ПОДХОДИТ**
   - **Проблема:** Стратегии активны только в определенных режимах
   - **Решение:** Проверить текущий режим рынка

## ✅ РЕШЕНИЯ

### ШАГ 1: Включить PAPER_TRADE_ASSIST

Добавьте в `.env`:
```bash
PAPER_TRADE_ASSIST=true
```

**Что это делает:**
- Ослабляет фильтры spread (2.5x более либерально)
- Снижает пороги volatility (50% ниже минимум, 50% выше максимум)
- Уменьшает edge threshold на 40%
- Укорачивает cooldown в 3 раза
- Увеличивает daily attempts в 2 раза
- Гарантирует минимум 2 открытых позиции

### ШАГ 2: Включить DEBUG_MODE (для тестирования)

Добавьте в `.env`:
```bash
DEBUG_MODE=true
```

**Что это делает:**
- Отключает volatility hard blocks (P99 → P95)
- Ослабляет volatility soft blocks (P90 → отключено)
- Позволяет торговлю в любых режимах
- Более либеральные фильтры

### ШАГ 3: Проверить логи

```bash
# Проверить, получает ли система тики
docker-compose logs hean-afo-engine | grep -i "tick\|TICK"

# Проверить, активны ли стратегии
docker-compose logs hean-afo-engine | grep -i "strategy.*started"

# Проверить блокировки
docker-compose logs hean-afo-engine | grep -i "blocked\|reject"
```

### ШАГ 4: Перезапустить систему

```bash
docker-compose restart hean-afo-engine
```

## 📋 ПОЛНЫЙ СПИСОК НАСТРОЕК ДЛЯ АКТИВНОЙ ТОРГОВЛИ

Добавьте в `.env`:

```bash
# Включить Paper Trade Assist (ослабляет фильтры)
PAPER_TRADE_ASSIST=true

# Включить Debug Mode (для тестирования)
DEBUG_MODE=true

# Увеличить лимиты на попытки
MAX_DAILY_ATTEMPTS_PER_STRATEGY=50

# Уменьшить cooldown
COOLDOWN_MINUTES=5

# Ослабить spread фильтры
IMPULSE_MAX_SPREAD_BPS=50

# Увеличить лимит открытых позиций
MAX_OPEN_POSITIONS=10
```

## 🔍 ДИАГНОСТИКА

Запустите диагностический скрипт:
```bash
python3 diagnose_trading_issue.py
```

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После применения настроек:
- **Сигналов в час:** 5-20 (вместо 0)
- **Ордеров в день:** 10-50 (вместо 2)
- **Конверсия сигналов:** 20-50% (вместо 0%)

## ⚠️ ВАЖНО

1. **DEBUG_MODE** - только для тестирования! Не используйте в production
2. **PAPER_TRADE_ASSIST** - безопасен только в paper/testnet режиме
3. После изменений **обязательно перезапустите** систему

## 🚀 БЫСТРОЕ ИСПРАВЛЕНИЕ

```bash
# 1. Добавить настройки в .env
echo "PAPER_TRADE_ASSIST=true" >> .env
echo "DEBUG_MODE=true" >> .env

# 2. Перезапустить
docker-compose restart hean-afo-engine

# 3. Проверить логи
docker-compose logs -f hean-afo-engine | grep -i "signal\|order"
```

## 📈 МОНИТОРИНГ

После применения исправлений, следите за:
- Количеством сигналов (должно увеличиться)
- Количеством ордеров (должно увеличиться)
- Причинами блокировок (должны уменьшиться)

```bash
# Проверить статистику через API
curl http://localhost:8000/api/analytics/no-trade-report | python3 -m json.tool
```

---

**Дата создания:** 2026-01-10  
**Статус:** Требует применения исправлений
